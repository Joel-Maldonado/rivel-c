func main() -> int {
    name := "Nova";
    n := 3;
    ratio := 0.5;
    ok := true;
    println(f"{name} has {n} items at {ratio} ({ok})");
    println(f"{{literal}} {n + 1} {f"nested {name}"}");
    println(f"");
    println(f"{"quoted"}");
    return 0;
}
