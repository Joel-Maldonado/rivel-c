fn main() -> Int {
    const base = "a"
    mut message = base

    if true {
        const message = message + "b"
        println(message)
    }

    message = message + "c"
    println(message)
    return len(message)
}
